 // myarray.js
var activeObj = 0;
var objArray = new Array();
var lineArray = new Array();


function getObj(x) {
     return objArray[x]
 }
function addObj(item) {
     objArray.push(item)
 }

function getLine(x) {
     return lineArray[x]
 }
function addLine(item) {
     lineArray.push(item)
 }

